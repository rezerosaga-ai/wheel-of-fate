'use client';
// This file is intentionally minimal — the Wheel of Fate game uses React components
// directly (GameRoom, WaitingRoom, etc.) instead of a game-engine HUD overlay.
// Kept as a stub so the template build does not break.
export default function HUD() {
  return null;
}
